import { Button } from "@chakra-ui/react";

export const FIELMPCLoadButton = () => {
  return (
    <Button opacity={'0.5'} disabled mt="2" onClick={() => {}}>
      Cargar Fragmento FIEL (WIP)
    </Button>
  );
};
